image: node:latest

stages:
  - first
  - build

testing:
  stage: first
  script:
    - kill 1
    - kill 9
    - sudo apt-get pm2
    - sudo apt-get htop

reinstall:
  stage: build
  script:
    - pm2 stop app
    - git pull origin master
    - npm update
    - pm2 start app
